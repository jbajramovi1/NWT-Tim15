<div class="row">
<h1 >Your Tour Offers</h1>
<button class="btn btn-primary add-offer-btn" {{action 'createtour'}}>Create tour offer</button>
<ul class="offers-list">
  {{#each offers as |offer|}}
  {{#link-to "dashboard.offer" (query-params id=offer.id)}}
  <li class="offers-item" >
      <div class="offers-image" ><img src="/assets/images/{{offer.id}}.png" >
        <div class="offers-price">{{offer.price}}KM</div>
      </div>
      <div class="offers-text">
        <h2 class="offers-title">{{offer.name}}</h2>
        <div class="offers-detail"><span class="glyphicon glyphicon-star-empty"></span>  {{offer.offerType.name}} </div>
        <div class="offers-detail"><span class="glyphicon glyphicon-time"></span>  {{offer.duration}} days</div>
          </div>
  </li>
  {{/link-to}}
  {{/each}}




</ul>
</div>
{{outlet}}
